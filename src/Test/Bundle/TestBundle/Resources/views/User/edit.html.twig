{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Modification de l'utilisateur {{ entity.prenom ~ entity.nom }}</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_user') }}">
            Annuler et retourner à la lsite
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
